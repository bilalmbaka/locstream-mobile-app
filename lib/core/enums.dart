enum Status { initial, loading, success, error, unverified, blocked, noNetwork }

enum CryptoPricesEvent { subscribed, price, error, ping, pong }

enum Sentiment { pending, cancelled, disputed, statisfied, rejected, accepted }
